#include <iostream>
#include <vector>
#include "../inc/easyfind.hpp"

using std::string;
using std::cout;
using std::endl;

int main()
{
	std::vector<int> test = {1, 2, 3, 4, 5, 6, 7, 8};
	test.push_back(1)
	easyfind(test, 5);
}