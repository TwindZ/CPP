#ifndef EASYFIND_HPP
# define EASYFIND_HPP

#include <iostream>
#include <exception>
#include <stdexcept>
#include <vector>

template <typename T>
void easyfind(T data, int toFind)
{
	

	for(T::iterator it = test.begin(); it != test.end(); it++)
	{
		std::cout << test << std::endl;
	} 
}
#endif